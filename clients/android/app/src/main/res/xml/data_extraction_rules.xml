<?xml version="1.0" encoding="utf-8"?>
<!--
   SSDID Drive Data Extraction Rules

   SECURITY: Disable all backup and extraction for security-sensitive app.
   We store encrypted private keys and tokens that should never leave the device.
-->
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude everything from cloud backup -->
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <!-- Exclude everything from device-to-device transfer -->
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
