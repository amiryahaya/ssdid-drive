name: PII Service Deploy

# =============================================================================
# DEPLOYMENT WORKFLOW
# =============================================================================
# - Staging: Triggered on push to main (services/pii_service/** changes)
# - Production: Triggered by tag push (pii-service-v*) or manual dispatch
# - Manual: Via workflow_dispatch with environment selector
#
# Required GitHub secrets (per environment):
#   STAGING_SSH_HOST, STAGING_SSH_USER, STAGING_SSH_KEY
#   PROD_SSH_HOST, PROD_SSH_USER, PROD_SSH_KEY
#   PII_DATABASE_URL, PII_SECRET_KEY_BASE
# See: docs/deployment/secrets.md for full list
# =============================================================================

on:
  push:
    branches: [main]
    paths:
      - 'services/pii_service/**'
      - '.github/workflows/pii-service-deploy.yml'
    tags:
      - 'pii-service-v*'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production
      version:
        description: 'Version tag (optional, uses SHA if empty)'
        required: false
        type: string

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/pii-service

defaults:
  run:
    working-directory: services/pii_service

jobs:
  # ============================================================================
  # Run CI First
  # ============================================================================
  ci:
    name: Run CI
    uses: ./.github/workflows/pii-service-ci.yml
    secrets: inherit

  # ============================================================================
  # Build & Push Docker Image
  # ============================================================================
  build:
    name: Build & Push
    runs-on: ubuntu-latest
    needs: [ci]
    permissions:
      contents: read
      packages: write
    outputs:
      image_tag: ${{ steps.meta.outputs.version }}
      image_digest: ${{ steps.build.outputs.digest }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}},prefix=v
            type=semver,pattern={{major}}.{{minor}},prefix=v
            type=sha,prefix=sha-
            type=raw,value=${{ inputs.version }},enable=${{ inputs.version != '' }}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build and push
        id: build
        uses: docker/build-push-action@v5
        with:
          context: services/pii_service
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILD_DATE=${{ github.event.head_commit.timestamp }}
            VCS_REF=${{ github.sha }}
            VERSION=${{ steps.meta.outputs.version }}

      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.build.outputs.digest }}
          artifact-name: sbom-pii-service.spdx.json
          output-file: ./sbom-pii-service.spdx.json

      - name: Upload SBOM
        uses: actions/upload-artifact@v4
        with:
          name: sbom-pii-service
          path: services/pii_service/sbom-pii-service.spdx.json

  # ============================================================================
  # Deploy to Staging
  # ============================================================================
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: [build]
    # Enable when staging is provisioned by removing 'false &&'
    if: false && (github.ref == 'refs/heads/main' || inputs.environment == 'staging')
    environment:
      name: staging
      url: ${{ vars.STAGING_URL }}
    concurrency:
      group: deploy-pii-staging
      cancel-in-progress: false

    steps:
      - uses: actions/checkout@v4

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.STAGING_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.STAGING_SSH_HOST }} >> ~/.ssh/known_hosts
        working-directory: .

      - name: Deploy to staging
        run: |
          ssh -i ~/.ssh/deploy_key ${{ secrets.STAGING_SSH_USER }}@${{ secrets.STAGING_SSH_HOST }} << 'DEPLOY_SCRIPT'
            set -e
            echo "=== Pulling latest PII service image ==="
            docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

            echo "=== Tagging previous image as rollback ==="
            docker tag ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest \
              ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:rollback 2>/dev/null || true

            echo "=== Running database migrations ==="
            docker run --rm --env-file /opt/pii-service/.env \
              --network host \
              ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest \
              bin/pii_service eval "PiiService.Release.migrate()"

            echo "=== Restarting service ==="
            cd /opt/pii-service
            docker compose pull
            docker compose up -d --remove-orphans

            echo "=== Cleanup old images ==="
            docker image prune -f
          DEPLOY_SCRIPT
        working-directory: .

      - name: Health check
        run: |
          echo "Waiting for PII service to become healthy..."
          for i in $(seq 1 30); do
            if curl -sf "${{ vars.STAGING_URL }}/health" > /dev/null 2>&1; then
              echo "Health check passed on attempt $i"
              exit 0
            fi
            echo "Attempt $i/30 - waiting 10s..."
            sleep 10
          done
          echo "::error::Health check failed after 30 attempts"
          exit 1
        working-directory: .

      - name: Rollback on failure
        if: failure()
        run: |
          echo "::warning::Deployment failed - attempting rollback"
          ssh -i ~/.ssh/deploy_key ${{ secrets.STAGING_SSH_USER }}@${{ secrets.STAGING_SSH_HOST }} << 'ROLLBACK'
            set -e
            cd /opt/pii-service
            docker tag ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:rollback \
              ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest 2>/dev/null || exit 0
            docker compose up -d --remove-orphans
            echo "Rollback complete"
          ROLLBACK
        working-directory: .

  # ============================================================================
  # Deploy to Production
  # ============================================================================
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [build]
    # Enable when production is provisioned by removing 'false &&'
    if: false && (startsWith(github.ref, 'refs/tags/pii-service-v') || inputs.environment == 'production')
    environment:
      name: production
      url: ${{ vars.PRODUCTION_URL }}
    concurrency:
      group: deploy-pii-production
      cancel-in-progress: false

    steps:
      - uses: actions/checkout@v4

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.PROD_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.PROD_SSH_HOST }} >> ~/.ssh/known_hosts
        working-directory: .

      - name: Deploy to production
        run: |
          IMAGE_TAG="${{ needs.build.outputs.image_tag }}"
          ssh -i ~/.ssh/deploy_key ${{ secrets.PROD_SSH_USER }}@${{ secrets.PROD_SSH_HOST }} << DEPLOY_SCRIPT
            set -e
            echo "=== Pulling image ${IMAGE_TAG} ==="
            docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${IMAGE_TAG}

            echo "=== Running database migrations ==="
            docker run --rm --env-file /opt/pii-service/.env \
              --network host \
              ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${IMAGE_TAG} \
              bin/pii_service eval "PiiService.Release.migrate()"

            echo "=== Restarting service ==="
            cd /opt/pii-service
            docker compose pull
            docker compose up -d --remove-orphans

            docker image prune -f
          DEPLOY_SCRIPT
        working-directory: .

      - name: Health check
        run: |
          for i in $(seq 1 30); do
            if curl -sf "${{ vars.PRODUCTION_URL }}/health" > /dev/null 2>&1; then
              echo "Health check passed on attempt $i"
              exit 0
            fi
            sleep 10
          done
          echo "::error::Production health check failed"
          exit 1
        working-directory: .

      - name: Rollback on failure
        if: failure()
        run: |
          echo "::error::Production deployment failed - attempting rollback"
          ssh -i ~/.ssh/deploy_key ${{ secrets.PROD_SSH_USER }}@${{ secrets.PROD_SSH_HOST }} << 'ROLLBACK'
            set -e
            cd /opt/pii-service
            docker tag ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:rollback \
              ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest 2>/dev/null || exit 0
            docker compose up -d --remove-orphans
          ROLLBACK
        working-directory: .

  # ============================================================================
  # Database Migration (Manual Trigger)
  # ============================================================================
  migrate:
    name: Run Migrations
    runs-on: ubuntu-latest
    needs: [build]
    if: false  # Run via workflow_dispatch when needed
    environment:
      name: ${{ inputs.environment || 'staging' }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          if [ "${{ inputs.environment }}" = "production" ]; then
            echo "${{ secrets.PROD_SSH_KEY }}" > ~/.ssh/deploy_key
            SSH_HOST="${{ secrets.PROD_SSH_HOST }}"
            SSH_USER="${{ secrets.PROD_SSH_USER }}"
          else
            echo "${{ secrets.STAGING_SSH_KEY }}" > ~/.ssh/deploy_key
            SSH_HOST="${{ secrets.STAGING_SSH_HOST }}"
            SSH_USER="${{ secrets.STAGING_SSH_USER }}"
          fi
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts
          echo "SSH_HOST=$SSH_HOST" >> $GITHUB_ENV
          echo "SSH_USER=$SSH_USER" >> $GITHUB_ENV
        working-directory: .

      - name: Run migrations
        run: |
          ssh -i ~/.ssh/deploy_key $SSH_USER@$SSH_HOST << 'MIGRATE'
            set -e
            docker run --rm --env-file /opt/pii-service/.env \
              --network host \
              ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest \
              bin/pii_service eval "PiiService.Release.migrate()"
          MIGRATE
        working-directory: .

  # ============================================================================
  # Create Release
  # ============================================================================
  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [build]
    if: startsWith(github.ref, 'refs/tags/pii-service-v')
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download SBOM
        uses: actions/download-artifact@v4
        with:
          name: sbom-pii-service

      - name: Generate changelog
        id: changelog
        run: |
          echo "## Changes" > CHANGELOG.md
          git log --pretty=format:"- %s (%h)" $(git describe --tags --abbrev=0 HEAD^)..HEAD >> CHANGELOG.md || echo "- Initial release" >> CHANGELOG.md
        working-directory: .

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: PII Service ${{ github.ref_name }}
          body_path: CHANGELOG.md
          files: |
            sbom-pii-service.spdx.json
          draft: false
          prerelease: ${{ contains(github.ref_name, 'rc') || contains(github.ref_name, 'beta') }}
